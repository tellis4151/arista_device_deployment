---
- name: Arista Set Hostname
  hosts: arista_routers
  gather_facts: no

  tasks: 
  - name: Set Router Hostname
    arista.eos.eos_hostname:
      config:
        hostname: arista-dev-router-1
      state: merged 
